import type { Meta, StoryObj } from '@storybook/react'

import AcceptPage from './AcceptPage'

const meta: Meta<typeof AcceptPage> = {
  component: AcceptPage,
}

export default meta

type Story = StoryObj<typeof AcceptPage>

export const Primary: Story = {}
