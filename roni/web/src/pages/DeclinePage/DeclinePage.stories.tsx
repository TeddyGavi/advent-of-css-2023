import type { Meta, StoryObj } from '@storybook/react'

import DeclinePage from './DeclinePage'

const meta: Meta<typeof DeclinePage> = {
  component: DeclinePage,
}

export default meta

type Story = StoryObj<typeof DeclinePage>

export const Primary: Story = {}
